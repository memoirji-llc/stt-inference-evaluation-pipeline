# Experiment: Whisper large-v3, 100 files, full duration (GPU)
# Large-scale benchmark with complete audio - MOST COMPREHENSIVE
# This will take significant time and resources

experiment_id: vhp-large-v3-sample100-full-gpu
model:
  name: "whisper-large-v3"
  dir: "./models/faster-whisper/models--Systran--faster-whisper-large-v3"
  batch_size: 8
  device: "cuda"
  compute_type: "float16"

input:
  source: "azure_blob"
  parquet_path: "data/raw/loc/veterans_history_project_resources.parquet"
  blob_prefix: "loc_vhp"
  sample_size: 100
  duration_sec: null  # Full audio
  sample_rate: 16000

output:
  dir: "outputs/vhp-large-v3-sample100-full-gpu"
  save_per_file: true

wandb:
  project: amia-stt
  group: experiments-gpu-large-v3
  tags: ["whisper-large-v3", "sample100", "full-duration", "gpu", "t4"]

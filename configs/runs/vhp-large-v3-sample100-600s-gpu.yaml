# Experiment: Whisper large-v3, 100 files, 10 minutes (GPU)
# Large-scale benchmark with longer audio

experiment_id: vhp-large-v3-sample100-600s-gpu
model:
  name: "whisper-large-v3"
  dir: "./models/faster-whisper/models--Systran--faster-whisper-large-v3"
  batch_size: 8
  device: "cuda"
  compute_type: "float16"

input:
  source: "azure_blob"
  parquet_path: "data/raw/loc/veterans_history_project_resources.parquet"
  blob_prefix: "loc_vhp"
  sample_size: 100
  duration_sec: 600  # 10 minutes
  sample_rate: 16000

output:
  dir: "outputs/vhp-large-v3-sample100-600s-gpu"
  save_per_file: true

wandb:
  project: amia-stt
  group: experiments-gpu-large-v3
  tags: ["whisper-large-v3", "sample100", "duration-600s", "gpu", "t4"]
